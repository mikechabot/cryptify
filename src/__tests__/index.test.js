import Cryptify from '../index';

describe('Cryptify', () => {
    test('is delicious', () => {
        expect(true).toBeTruthy();
    });
    test('is not sour', () => {
        expect(false).toBeFalsy();
    });
});
